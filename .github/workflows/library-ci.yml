name: Library CI
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Scripts
        run: |
          docker build -t library-v1.0:latest .
          docker login -u akshaysdev -p dckr_pat_0rSJxel4DVFMZ4bEVBWjbHK4fNM
          docker tag library-v1.0:latest akshaysdev/
          docker push akshaysdev/library-v1.0
          docker pull akshaysdev/library-v1.0:latest
